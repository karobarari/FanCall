// Single source of truth for which club this deployment is piloting.
// Every account is assigned this team automatically at signup — there's no
// picker right now. Multi-club support (letting a user choose from the
// seeded teams) returns in roadmap step 21; until then, changing clubs
// means changing this one constant (plus the branding in P3).
export const PILOT_TEAM_NAME = 'Manchester City';
