# Must exactly match the home_team/away_team string used for fixtures
# (and the backend's PILOT_TEAM_NAME in server/src/config/pilotTeam.ts) —
# drives the WIN/DRAW/LOSE framing in src/lib/result.ts.
VITE_CLUB=Manchester City
VITE_API_URL=http://localhost:3000/api